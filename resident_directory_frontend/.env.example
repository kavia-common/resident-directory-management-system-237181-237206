# Backend API URL - points to the FastAPI backend (port 3001)
# NEXT_PUBLIC_API_BASE is set by the deployment platform automatically
NEXT_PUBLIC_API_BASE=http://localhost:3001

# Local development override (used if NEXT_PUBLIC_API_BASE is not set)
NEXT_PUBLIC_API_URL=http://localhost:3001
