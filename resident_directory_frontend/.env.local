# Backend API URL - FastAPI backend running on port 3001
# NEXT_PUBLIC_API_BASE is used by the platform deployment (takes priority in api.ts)
# NEXT_PUBLIC_API_URL is the local development fallback
NEXT_PUBLIC_API_URL=http://localhost:3001
